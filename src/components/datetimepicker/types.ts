export type DatePickerPropsType = {
  isRange?: boolean;
  onChange: any;
  format?: string;
  placeHolder?: string;
  value?: any;
  disabled?: boolean;
  showTime?: boolean;
  rangeValue?: string[];
  rangePlaceHolder?: [string, string];
  className?: string;
};
